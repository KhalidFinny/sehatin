{
  "builds": [
    {
      "src": "apps/client/angular.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist/sehatin/browser"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}